/**
 * Chessio UI Components
 *
 * Simplified primitives using Chessio semantic design tokens.
 * No external dependencies - just Tailwind classes.
 *
 * Components:
 * - Button: Primary, secondary, ghost, destructive variants
 * - Input: Text input with label and error support
 * - Card: Container with header, content, footer, title
 * - Badge: Status indicators (default, success, warning)
 * - Dialog: Modal dialogs with title and close button
 */

export { Button } from "./Button";
export { Input } from "./Input";
export { Card, CardHeader, CardContent, CardFooter, CardTitle } from "./Card";
export { Badge } from "./Badge";
export { Dialog } from "./Dialog";
